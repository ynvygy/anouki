<!DOCTYPE html>
<html>
<head>
  <title>Anouki</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
	<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">Anouki.Art</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav pull-right">
        <% if user_signed_in? && current_user.admin %>
           <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Admin Panel<span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to "Create new post", new_post_path%></li>
              <li><%= link_to "User List", userlist_path%></li>
              <li><%= link_to "Work Categories", workcategories_path%></li>
            </ul></li>
        <% end %>
        <li><%= link_to "Home", root_path%></li>
        <li><%= link_to "Blog", posts_path%></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">My Work <span class="caret"></span></a>
          <ul class="dropdown-menu">
              <% unless @workcategory.nil? %>
                <% @workcategory.each do |workcategory| %>
                  <li><%= link_to workcategory.name, workcategory_drawings_path(workcategory.id)%></li>
                <% end %>
              <%end%>
            <li role="separator" class="divider"></li>
            <li><a href="#">Events</a></li></ul></li>
        <li><%= link_to "About Me", about_path %></li>
        <li><%= link_to "Contact", contacts_path%></li>  
        <% if !user_signed_in? %>    
            <li class="active"><li><%= link_to "Login", new_user_session_path %></li>
        <% end %>
        <% if user_signed_in? %>
            <li class="active"><li><%= link_to "Logout", destroy_user_session_path, :method => :delete %></li>
        <% end %>

      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

<% flash.each do |name, msg| %>
  <%= content_tag(:div, msg, class: "alert") %>
<% end %>

<%= yield %>

</body>
</html>
