
  <div class="form-group">
	<%= form_for([@post,@post.comments.build]) do |f| %>
	<div class="col-sm-offset-1 col-sm-4">
		<p>
			<%= f.label :body %><br>
			<%= f.text_field :body %>
		</p>
		<p>
			<%= f.submit %>
		</p>
	</div>
<%end%>
</div>


